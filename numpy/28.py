#Get the data type of an array containing strings :

import numpy as np

arr = np.array(['apple','banana','cherry'])

print(arr.dtype)

# u :This indicates that the data type is a unicode string.
# 6 : This specifies the length of the Unicode strings in the array.