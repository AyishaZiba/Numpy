#Sorting Arrays

#Sorting means putting elements in an orders sequence .
#sort(), that will sort a specified array.

#Sort the array?

import numpy as np

arr = np.array([3,2,0,1])
print(np.sort(arr))

#Note : THis method returns a copy of the array.
# leaving the original array unchanged.
